export type NewsSectionProps = {
  text?: string;
};

export type NewsPiece = {
  id: string;
  author: string;
  avatarUrl?: string;
  readingTime: string;
  shortText: string;
  title?: string;
  text?: string;
};
